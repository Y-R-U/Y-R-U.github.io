Third-party code is vendored here. **Nothing is ever loaded from a CDN** — a CDN
import has silently hung every other 3D game in this repo with zero console errors.

Empty as of P1: the engine has no third-party dependencies.
