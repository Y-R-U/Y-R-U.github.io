# docs/refs/poster — before/after evidence for `art/tools/poster.js`

Agent G, 2026-08-24. Analysis and the exact commands are in `../../ART_PROPS.md`.

Every "before" here is the **same** crop → key → resize → trim with `--bypass 1`, so a pair isolates
`poster.js` and nothing else.

| file | what |
|---|---|
| `t01_hangar_before/after.png` | the worst plate in the project (`probes_ab/t01_hangar_oldgrammar`), `--preset struct` |
| `t10_aagun_before/after.png` | the best prop so far (`probes_ab/t10_aagun_twotone_noassetphrase_4b`), `--preset mech` |
| `before_after.png` | those four in one sheet, on paper |
| `in_situ.png` | the same four on painted ground (`probes/p06_ground_fields`), which is the harder and more honest test |
| `prop_sheet.png` | eight baked TERRAIN props — the blind-critic sheet |
| `prop_sheet_ground.png` | the same eight on painted ground |

Rebuild any of them with `art/tools/bake.js` and `art/tools/sheet.js`; nothing here is hand-edited.
