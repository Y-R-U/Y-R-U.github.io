// Onboarding copy and the opening objective chain. Data only — no DOM, no three, no Math.random,
// so js/sim/ style purity rules apply here too.

export default Object.freeze({
  cards: Object.freeze([]),
  objectives: Object.freeze([]),
});
