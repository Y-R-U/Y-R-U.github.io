// Combat — STUB. Owner: see CLAUDE.md.
//
// Bullets and damage. A pooled instanced tracer set, `GUN.fireCap` visible
// shooters standing in for the whole squad, and the hit resolution that feeds
// gates, barriers, enemies and the boss.
//
// Replace this file wholesale. Keep the export names — main.js and game.js
// import them by name and the whole build boots off that contract.

export function initCombat(ctx) { return null; }
export function resetCombat(level) { return null; }
export function updateCombat(dt) { return null; }
export function disposeCombat() { return null; }
export function fireBurst(x, y, z, dir) { return null; }
