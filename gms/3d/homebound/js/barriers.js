// Barriers — STUB. Owner: see CLAUDE.md.
//
// The numbered wooden walls from the reference frames: a blocker with HP that
// counts down under fire and costs you men if you body it while it still stands.
//
// Replace this file wholesale. Keep the export names — main.js and game.js
// import them by name and the whole build boots off that contract.

export function initBarriers(ctx) { return null; }
export function resetBarriers(level) { return null; }
export function updateBarriers(dt) { return null; }
export function barrierHitTest(x, y, z, r) { return null; }
export function disposeBarriers() { return null; }
