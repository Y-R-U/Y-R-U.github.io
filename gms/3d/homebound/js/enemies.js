// Enemies — STUB. Owner: see CLAUDE.md.
//
// The wall of red: enemy blocks, marching columns, skirmishers and the chapter
// bosses. Same crowd tech as the player army, opposite colour, hostile fire.
//
// Replace this file wholesale. Keep the export names — main.js and game.js
// import them by name and the whole build boots off that contract.

export function initEnemies(ctx) { return null; }
export function resetEnemies(level) { return null; }
export function updateEnemies(dt) { return null; }
export function enemyHitTest(x, y, z, r) { return null; }
export function disposeEnemies() { return null; }
export function enemyCount() { return null; }
export function bossState() { return null; }
