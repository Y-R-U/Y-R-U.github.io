// Levels — STUB. Owner: see CLAUDE.md.
//
// Level generation. Deterministic from a seed, laid out against the chapter's
// difficulty curve, with hand-authored beats layered on top.
//
// Replace this file wholesale. Keep the export names — main.js and game.js
// import them by name and the whole build boots off that contract.

export function buildLevel(spec) { return null; }
export function levelSpec(chapter, level) { return null; }
export const LEVEL_KINDS = [];
