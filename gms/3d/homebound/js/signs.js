// Signs — STUB. Owner: see CLAUDE.md.
//
// Every painted surface in the game: gate faces, barrier numbers, button plates.
// Canvas textures, cached by key — a level with forty +1 gates allocates one.
//
// Replace this file wholesale. Keep the export names — main.js and game.js
// import them by name and the whole build boots off that contract.

export function initSigns() { return null; }
export function signTexture(spec) { return null; }
export function panelMaterial(kind, sign) { return null; }
export function barrierTexture(value) { return null; }
export function disposeSigns() { return null; }
