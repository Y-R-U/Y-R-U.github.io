// Units — STUB. Owner: see CLAUDE.md.
//
// One geometry per tier on the ladder, built from tagged boxes (see toon.js
// `partBox`/`mergeParts`) so the crowd shader can walk them without bones.
//
// Replace this file wholesale. Keep the export names — main.js and game.js
// import them by name and the whole build boots off that contract.

export function initUnits(ctx) { return null; }
export function tierGeometry(tierId) { return null; }
export function makeTierCrowd(tierIndex, opts) { return null; }
export function disposeUnits() { return null; }
