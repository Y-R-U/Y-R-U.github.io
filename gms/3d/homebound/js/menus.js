// Menus — STUB. Owner: see CLAUDE.md.
//
// The main screen with its autoplay backdrop and side rail, the level select,
// the outro, and every popup.
//
// Replace this file wholesale. Keep the export names — main.js and game.js
// import them by name and the whole build boots off that contract.

export function initMenus() { return null; }
export function showMain() { return null; }
export function showOutro(result) { return null; }
export function hideAll() { return null; }
export function updateMenus(dt) { return null; }
