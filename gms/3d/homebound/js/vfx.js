// Vfx — STUB. Owner: see CLAUDE.md.
//
// Muzzle flash, impact sparks, explosions, smoke, debris, floating numbers and
// screen shake. All pooled instanced quads; nothing here allocates in a frame.
//
// Replace this file wholesale. Keep the export names — main.js and game.js
// import them by name and the whole build boots off that contract.

export function initVfx(ctx) { return null; }
export function resetVfx() { return null; }
export function updateVfx(dt) { return null; }
export function disposeVfx() { return null; }
export function explode(pos, scale, color) { return null; }
export function floatNumber(pos, text, color) { return null; }
