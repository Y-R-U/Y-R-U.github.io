// Audio — STUB. Owner: see CLAUDE.md.
//
// WebAudio, synthesised — no asset downloads. Gunfire, impacts, gate chimes,
// promotion stings, and a low loop under the run.
//
// Replace this file wholesale. Keep the export names — main.js and game.js
// import them by name and the whole build boots off that contract.

export function initAudio() { return null; }
export function sfx(name, opts) { return null; }
export function music(track) { return null; }
export function setMuted(b) { return null; }
