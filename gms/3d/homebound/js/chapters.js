// Chapters — STUB. Owner: see CLAUDE.md.
//
// The five chapters, their lengths, their curves and their unlocks.
//
// Replace this file wholesale. Keep the export names — main.js and game.js
// import them by name and the whole build boots off that contract.

export const CHAPTERS = [];
export function chapterOf(n) { return null; }
export function levelCount(chapter) { return null; }
export function reqPowerFor(chapter, level) { return null; }
export function rewardFor(chapter, level) { return null; }
