// Gates — STUB. Owner: see CLAUDE.md.
//
// Gate panels and the three verbs: GROW under fire, BREAK on glass, PRESS on a
// button plate. Owns pass-through detection and emits gate:* on the bus.
//
// Replace this file wholesale. Keep the export names — main.js and game.js
// import them by name and the whole build boots off that contract.

export function initGates(ctx) { return null; }
export function resetGates(level) { return null; }
export function updateGates(dt) { return null; }
export function gateHitTest(x, y, z, r) { return null; }
export function disposeGates() { return null; }
export function gateChoices(z) { return null; }
