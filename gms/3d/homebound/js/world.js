// World — STUB. Owner: see CLAUDE.md.
//
// The road corridor: tarmac, painted lines, rock banks, the river either side,
// parapet walls, distant hills and whatever decor the theme calls for. Segments
// are pooled and recycled as the squad advances; nothing is ever built per frame.
//
// Replace this file wholesale. Keep the export names — main.js and game.js
// import them by name and the whole build boots off that contract.

export function initWorld(ctx) { return null; }
export function resetWorld(level) { return null; }
export function updateWorld(dt) { return null; }
export function disposeWorld() { return null; }
