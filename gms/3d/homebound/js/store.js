// Store — STUB. Owner: see CLAUDE.md.
//
// Base upgrades bought between runs, and the house store once HOME exists.
//
// Replace this file wholesale. Keep the export names — main.js and game.js
// import them by name and the whole build boots off that contract.

export function initStore() { return null; }
export function showStore() { return null; }
export function hideStore() { return null; }
