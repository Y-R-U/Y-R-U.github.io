// Story — STUB. Owner: see CLAUDE.md.
//
// Short. One or two bubbles a level, never more.
//
// Replace this file wholesale. Keep the export names — main.js and game.js
// import them by name and the whole build boots off that contract.

export function initStory() { return null; }
export function beatsFor(chapter, level) { return null; }
export function playBeat(beat) { return null; }
export function introBeats() { return null; }
