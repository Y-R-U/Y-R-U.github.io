// Hud — STUB. Owner: see CLAUDE.md.
//
// The in-run HUD: troop counter with its delta ticker, progress bar, boss bar,
// promotion banner, toasts, story bubbles and the damage flash.
//
// Replace this file wholesale. Keep the export names — main.js and game.js
// import them by name and the whole build boots off that contract.

export function initHud() { return null; }
export function resetHud(level) { return null; }
export function updateHud(dt) { return null; }
export function showHud(on) { return null; }
